# مهمّتك: نشر واجهة 9ri3a على هذا الخادم

أنت تعمل داخل الخادم نفسه (`84.8.223.62`). هدفك: واجهة برمجية تعمل على
`https://9ri3a.centrefocus.ma` وتقرأ من قاعدة PostgreSQL الحقيقية.

**الدليل الكامل موجود في `deploy/RUNBOOK.ar.md` — اقرأه كاملاً قبل أن تبدأ،
ثم نفّذ خطواته بالترتيب من 1 إلى 9.** الدليل مُختبَر: لا ترتجل بديلاً عنه، ولا
تقفز خطوة، ولا تتجاهل أمر التحقّق في نهاية كل خطوة. إن فشل أمر تحقّق، توقّف
وشخّص السبب قبل المتابعة.

---

## معطيات جاهزة (لا تسأل عنها)

| | |
|---|---|
| المستودع | `https://github.com/yasserox171/e_commerceApp.git` (عام، بلا مصادقة) |
| الفرع | `claude/ecommerce-wholesale-dropshipping-je8lgh` — وهو الفرع الافتراضي |
| النطاق | `9ri3a.centrefocus.ma` — معمّر مسبقاً في `Caddyfile` و `.env.production.example` |
| عنوان الخادم | `84.8.223.62` |
| مجلّد التثبيت | `/srv/qri3a` |
| ملف الأسرار | `/etc/qri3a/api.env` |

---

## اسأل المستخدم عن هذه — ولا تخمّنها أبداً

1. **اسم قاعدة البيانات** التي تحوي جدول `public.products`.
   تحقّق أولاً بنفسك ثم اعرض ما وجدت للتأكيد:
   ```bash
   sudo -u postgres psql -c "\l"
   sudo -u postgres psql -d <القاعدة> -c "SELECT count(*) FROM public.products;"
   ```
2. **هل PostgreSQL على هذا الخادم نفسه؟** إن كان على خادم آخر، فـ
   `DATABASE_URL` يتغيّر و `DATABASE_SSL=require` يصبح مطلوباً.
3. **تأكيد أن سجلّ `A` لـ `9ri3a.centrefocus.ma` يشير إلى `84.8.223.62`**
   قبل الخطوة 9. تحقّق:
   ```bash
   dig +short 9ri3a.centrefocus.ma
   ```
   إن لم يُرجع `84.8.223.62`، **توقّف** واطلب من المستخدم ضبط الـ DNS. Caddy
   لا يستطيع استخراج الشهادة قبل ذلك، وسيفشل بشكل متكرّر.

كلمتا المرور اللتان ستولّدهما (`openssl rand`) اعرضهما للمستخدم مرّة واحدة
واطلب منه حفظهما.

---

## قواعد صارمة

- **لا تكتب أي سرّ داخل `/srv/qri3a`.** الأسرار كلها في `/etc/qri3a/api.env`
  بصلاحية `0640 root:qri3a`. لا تنشئ `backend/api/.env` على هذا الخادم.
- **لا تُضعف حارس الإنتاج.** الواجهة ترفض الإقلاع إذا بقي `JWT_SECRET` على
  قيمته النموذجية أو كان `PUBLIC_API_URL` بـ `http`. إن رأيت هذا الرفض فهو
  يعمل كما صُمّم: صحّح القيمة، ولا تعدّل الكود ولا `reviewProductionConfig`.
- **`payments.configured: false` ليست مشكلة.** مفاتيح CMI غير متوفّرة بعد،
  وهذا مقصود. الكتالوج والحسابات والسلة والطلبات كلها تعمل، و
  `POST /payments/checkout` وحده يردّ `503`. لا تحاول إصلاحها ولا تضع مفاتيح
  وهمية ولا تبدّل إلى Stripe.
- **لا تشغّل `npm install` في جذر المستودع.** هذا يجرّ أدوات Expo و Android
  (حوالي 1 GB). `deploy.sh` يثبّت حزم الواجهة وحدها (~138 حزمة).
- **لا تعدّل ملفاً في `db/migrations/` طُبّق من قبل.** مدير الترحيلات يحفظ
  بصمة SHA-256 ويرفض التغيير. إن احتجت تعديلاً، اكتب ترحيلة جديدة.
- **لا تفتح المنفذ 4000 للخارج.** الواجهة مربوطة على `127.0.0.1` عمداً.
- **لا تعطِ الدور `qri3a_api` صلاحية الكتابة على `public`.** قراءة فقط —
  هذا ما يمنع أي خطأ في الواجهة من إتلاف كتالوج المنتجات.
- إن طلب الدليل `sudo`، استعمله. لكن **لا تشغّل `deploy.sh` كـ root** —
  يعمل بـ `sudo -H -u qri3a`، وإلا امتلأ `/srv/qri3a` بملفات يملكها root.

---

## المنافذ

هذا خادم **Oracle Cloud (OCI)**. الـ Security List في لوحة التحكّم **مضبوطة
مسبقاً** ولا تحتاج تغييراً: `22` و `80` و `443` مفتوحة من `0.0.0.0/0`.

لكن OCI فيه **جداران ناريان**، والثاني هو ما يُنسى: صور Ubuntu على OCI تأتي
بقواعد `iptables` مثبّتة تحجب كل شيء عدا `22`، مستقلّة تماماً عن الـ Security
List. اقرأ **الخطوة 8** في الدليل ونفّذها كما هي — فيها تشخيص أيّ جدار يعمل
فعلاً، والأمر الصحيح لكل حالة.

نقطتان لا تتجاوزهما:

- **لا تشغّل `ufw enable` إن كانت قواعد iptables مسبقة موجودة و `ufw` غير
  نشط.** الاثنان يتنازعان على نفس السلاسل، والنتيجة المعتادة قطع جلسة SSH
  الجارية — أي فقدان الوصول إلى الخادم.
- **أدرج قواعد iptables قبل قاعدة `REJECT`، لا بعدها.** الإضافة بعدها بلا أثر،
  وهذا يبدو كنجاح ثم تفشل الشهادة بلا سبب ظاهر.

**تبقى مغلقة عمداً:** `4000` (الواجهة على loopback) و `5432` (القاعدة محلّية).

الاختبار الوحيد المعتبر هو من **خارج** الخادم — `curl localhost` لا يثبت شيئاً،
لأن Let's Encrypt تتصل من الإنترنت:

```bash
curl -sI http://9ri3a.centrefocus.ma/
```

المنافذ `8000` و `8080` و `8090` مفتوحة أيضاً في الـ Security List. لسنا
نستعملها. اعرض على المستخدم ما يستمع عليها فعلاً ودعه يقرّر:

```bash
sudo ss -lntp | grep -E ':(8000|8080|8090)\b'
```

---

## المهمّة منتهية عندما

1. ‏`systemctl is-active qri3a-api` يقول `active`، و `systemctl is-enabled`
   يقول `enabled` (يقلع تلقائياً بعد إعادة تشغيل الخادم).
2. من **خارج** الخادم: `curl -s https://9ri3a.centrefocus.ma/health` يُرجع
   `"status":"ok"` و `"database":{"ok":true}` بشهادة صالحة.
3. الكتالوج يُرجع منتجات حقيقية، لا مصفوفة فارغة:
   ```bash
   curl -s "https://9ri3a.centrefocus.ma/products?channel=dropshipping" | head -c 300
   ```
   إن كانت فارغة، فالسبب غالباً أن كل قيم `status` مدرجة في
   `PRODUCT_HIDDEN_STATUSES`. افحص:
   ```bash
   sudo -u postgres psql -d <القاعدة> -c "SELECT status, count(*) FROM public.products GROUP BY status;"
   ```
   وعدّل القائمة في `/etc/qri3a/api.env` بدل تعديل الكود.
4. ‏`sudo -H -u qri3a /srv/qri3a/deploy/deploy.sh` ينتهي بنجاح — هذا يثبت أن
   التحديثات اللاحقة ستعمل، وهو ما سيستعمله المستخدم من الآن فصاعداً.
5. فهارس البحث منشأة (الخطوة 3.1) — اختيارية لكن نفّذها.

في النهاية اكتب للمستخدم تقريراً قصيراً يذكر: اسم القاعدة المستعملة، أين حُفظت
الأسرار، أي خطوة تخطّيتها ولماذا، وما تبقّى عليه (مفاتيح CMI، إعادة بناء الـ
APK على العنوان الجديد — الخطوة 12 في الدليل).

**لا تنشئ pull request ولا تدفع أي شيء إلى المستودع.** مهمّتك تشغيل الخادم، لا
تعديل الكود. إن اكتشفت خطأً حقيقياً في الكود أو الدليل، اشرحه للمستخدم بدل أن
ترقّعه محلّياً — التصحيح يجب أن يعود إلى المستودع لا أن يعيش على الخادم وحده.
